# Umgebungsvariablen — Panda-Bande Kinderevents
# Kopiere diese Datei nach .env.local und trage echte Werte ein.

# ─── Öffentliche Website-URL ─────────────────────────────────
# Steuert Canonical URLs, Sitemap, OpenGraph, E-Mail-Links und PDF-Logo-Auflösung.
# Nach Domain-Wechsel in Vercel setzen und neu deployen.
NEXT_PUBLIC_SITE_URL=https://ihre-domain.de

# Feste Produktions-URL für E-Mail-Bilder (Logo etc.) — nie Vercel-Preview verwenden
EMAIL_ASSET_BASE_URL=https://www.pb-kinderevents.de

# ─── Supabase ───────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ─── E-Mail (Resend) ────────────────────────────────────────
# API-Key von https://resend.com — Absender/Reply-To werden im CMS konfiguriert
# (Einstellungen → E-Mail). Absender-Fallback: info@pb-kinderevents.de (Panda-Bande).
RESEND_API_KEY=re_your_api_key
# Optional: Fallback für Benachrichtigungen, wenn im CMS leer
INQUIRY_NOTIFICATION_EMAIL=info@pb-kinderevents.de

# ─── Admin ──────────────────────────────────────────────────
ADMIN_PASSWORD=change-me-to-a-strong-password
